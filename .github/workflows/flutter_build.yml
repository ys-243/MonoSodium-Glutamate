name: Flutter Release Build

on:
  workflow_dispatch:

jobs:
  # --- ANDROID BUILD JOB ---
  build-android:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v3
        with:
          distribution: 'zulu'
          java-version: '17' # Flutter requires Java 17 for newer Android Gradle builds

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: Get Dependencies
        run: flutter pub get

      - name: Build APK
        run: flutter build apk --release

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: release-apk
          path: build/app/outputs/flutter-apk/app-release.apk

  # --- IOS BUILD JOB --- UNCOMMENT WHEN WE WANT TO BUILD THE IOS APP.
  # build-ios:
  #   runs-on: macos-latest
  #   steps:
  #     - name: Checkout Source Code
  #       uses: actions/checkout@v4

  #     - name: Setup Flutter
  #       uses: subosito/flutter-action@v2
  #       with:
  #         channel: 'stable'

  #     - name: Get Dependencies
  #       run: flutter pub get

  #     - name: Build iOS (Without Code Signing)
  #       # We use --no-codesign here to prevent Apple certificate errors
  #       run: flutter build ios --release --no-codesign

  #     - name: Upload iOS App
  #       uses: actions/upload-artifact@v4
  #       with:
  #         name: release-ios
  #         path: build/ios/iphoneos/Runner.app